spring:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5433/codecrack
    driver-class-name: org.postgresql.Driver
    username: codecrack
    password: codecrack123
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: localhost
      port: 6379
      password:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

logging:
  level:
    com.codecrack: INFO